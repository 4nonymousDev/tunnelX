# SSH 隧道管理器 — 设计文档

[English](DESIGN.en.md)

> 本文记录架构决策及其理由。实现已完成并通过实机验证。
>
> 使用说明见 [README.md](README.md)，构建说明见 [devhelper/README.md](devhelper/README.md)。

## 1. 目标与约束

把一套 PowerShell 脚本重做成带 UI 的工具，用于快速在任意目标机上搭建端口映射，方便远程开发调试。原脚本的问题见 §7。

**用途定位**：运维工具。把内网设备/服务通过一台公网服务器暴露给授权的远程开发者，与 Dev Tunnels、frp、ngrok 属同类。

### 硬约束

| 约束 | 含义 |
|---|---|
| 单文件 | 一个 exe 包含全部代码和依赖 |
| 免安装 | 拷过去双击即用 |
| 无运行时依赖 | 目标机不需要装 .NET / Node / OpenSSH / WebView |
| 小巧 | 体积尽量小 |

目标机是临时的/别人的开发机，不能假设上面装了任何东西。

### 软需求

- UI 用于填写配置信息
- 输出**可辨别**的故障调试日志（现有脚本最弱的一环）
- 后续需要服务端程序传递信息（如可用端口列表）

---

## 2. 技术选型

### 结论：Go + Fyne

| 层 | 选择 | 理由 |
|---|---|---|
| 语言 | **Go** | 静态编译真单文件、无运行时依赖，直接命中硬约束 |
| SSH | **`golang.org/x/crypto/ssh`** | 纯 Go 实现，**目标机不需要装 OpenSSH** |
| GUI | **Fyne** | 纯 Go 原生控件，不依赖 WebView/浏览器，独立窗口 |
| 配置 | JSON，放 exe 同目录 | 便携，跟着 exe 走 |

预计体积 ~15-25MB 单文件。

### 选型过程（备查）

淘汰的方案：

| 方案 | 淘汰原因 |
|---|---|
| Electron | 单文件也 80MB+，"小巧"不成立 |
| C# WPF/WinForms | 依赖机器已装 .NET，或 self-contained 后 60-150MB |
| Tauri / Wails | 依赖系统 WebView2（Win11 自带，但不是零依赖） |
| 内嵌 Web UI + 系统浏览器 | 体积更小（~10MB），但会弹浏览器标签页，观感不像桌面工具；且浏览器标签与进程生命周期错位需额外处理 |

**为什么最终选 Fyne 而非 Web UI**：需求是"类似 C# + WinForms 的小程序"——要独立窗口、双击就在。Fyne 下窗口生命周期与进程生命周期一致（关窗即关窗、最小化即最小化），与 WinForms 心智模型相同，且不引入"目标机默认浏览器是什么"这个变量。体积从 ~10MB 涨到 ~20MB，不触碰任何硬约束。

**Fyne 的代价**（已接受）：
- 编译需要 CGO（Windows 开发机要装 MinGW-w64/TDM-GCC）——**一次性开发机配置，不影响目标机**
- Material Design 风格，非 Win32 原生观感
- 控件比 WinForms 少，但表单/表格/日志够用

---

## 3. 系统架构

### 三方拓扑

```
        机器A（被访问方）                 服务器 S              机器B（访问方）
     ┌──────────────────┐          ┌──────────────┐      ┌──────────────────┐
     │ 本地服务 :80     │          │              │      │  浏览器          │
     │                  │          │              │      │    ↓ 访问        │
     │ [Exporter 模式]  │══SSH 22══▶│  :8080 ◄──┐  │      │  localhost:80    │
     │  -R 0:lo:80      │          │           │  │      │       ↑          │
     │                  │          │           │  │      │ [Importer 模式]  │
     │ [控制 channel]───┼──────────┼─▶ 注册表  │  │      │  -L 80:lo:8080   │
     │  "我是A,80→8080" │          │  A:80→8080│  │      │                  │
     └──────────────────┘          │           │◄═╪══SSH 22══[控制 channel]│
                                   └──────────────┘      └──────────────────┘
```

### 两种模式，一个程序

| 模式 | 作用 | SSH 原语 | x/crypto/ssh API |
|---|---|---|---|
| **Exporter** | 把本地端口推到服务器 | `-R` 远程转发 | `client.Listen()` |
| **Importer** | 把服务器端口拉到本地 | `-L` 本地转发 | `net.Listen()` + `client.Dial()` |

两者共用同一份 SSH 连接代码、UI、日志系统，方向相反。**是一个 exe 里的两种隧道类型，不是两个程序。** UI 上用"类型"列区分。

> 注：Importer 不需要自己写代理逻辑。`-L` 本地转发原生就是"把访问本地端口的流量转到服务端"。

---

## 4. 关键设计决策

### 4.1 控制通道走 SSH channel，服务器只开 22

**决策**：客户端与服务端的控制通信（查询在线 Exporter、上报映射关系）复用已建立的 SSH 连接，在其中开自定义 channel。**不额外监听任何端口。**

**背景**：SSH 是多路复用协议。一条 TCP 连接（22 端口）之上可以承载任意多个逻辑 channel，端口转发、shell、exec、自定义 channel 并行不悖，共用同一次认证和同一层加密。因此"22 被隧道占用了，控制通信得另开端口"这个前提**不成立**。

**为什么这是安全上的严格改进**：

| 另开端口 | 走 SSH channel |
|---|---|
| 多一个暴露的攻击面 | 服务器防火墙只需开 22 |
| 要自己实现认证 | 复用 SSH 公钥认证，**不写一行认证代码** |
| 要自己配 TLS | 复用 SSH 传输层加密 |
| 协议解析 bug = 任何人可打 | 协议解析 bug = 仅已授权用户可打 |

自研认证代码是最容易写出漏洞的地方。走 SSH channel 把这部分责任交给了 OpenSSH。

**实现**：客户端 `conn.OpenChannel("tunnel-ctrl@devhelper", nil)`，服务端在 `chans` 循环里识别该类型。

> **服务端必须为每条 channel 另起 goroutine，不得在 `chans` 循环里同步处理。**
>
> 控制通道的处理函数会阻塞至通道关闭（它在 `Recv` 循环里等消息）。若同步调用，
> 该连接的 channel 循环便再也读不到后续请求——Importer 随后发来的 `direct-tcpip`
> 会永远排队。症状是**连接建立后挂起、服务端毫无日志**，极难定位。
>
> 多路复用的要义正在于此：同一连接上的各 channel 必须并行，任何一方都不得阻塞
> 另一方。由 `internal/server/sameconn_test.go` 守护——该测试在同一条连接上同时
> 使用控制通道与转发通道，是唯一能暴露此缺陷的场景。

#### 部署形态：独立 sshd，而非复用系统 sshd

**决策：`tunnel-server` 自行监听一个端口（默认 2222），用 `x/crypto/ssh` 实现服务端，不使用系统 sshd。**

**原因**：系统 sshd 无法识别自定义 channel 类型。复用 22 的话，控制通道只能退化为"客户端用 SSH exec 跑一个服务端程序拿 JSON"——**这样服务端无法主动推送**，而主动推送是 §8.6（对端离线通知）与 §5.2（注册表变化广播）的前提。

**对"不多开端口"原则的影响**：

原则本身（§4.7）没有松动——真正要守的是**所有访问都必须先通过 SSH 认证**，而非"端口号必须是 22"。独立 sshd 仍然：

| 安全属性 | 是否保持 |
|---|---|
| 公钥认证，不自写认证代码 | ✅ 复用 `x/crypto/ssh` 服务端实现 |
| 传输层加密 | ✅ 同上 |
| 协议解析 bug 仅已授权用户可打 | ✅ 认证在前，控制消息在后 |
| 不额外暴露明文/无认证端口 | ✅ 2222 上跑的仍是 SSH 协议 |

代价仅是防火墙多放行一个端口，且该端口上的服务与 22 同等强度。这与"另开一个自写协议的 HTTP/WS 端口"有本质区别——后者才是 §4.1 所否决的。

**部署建议**：
- 系统 sshd 保持在 22 用于常规运维登录，二者互不干扰
- `tunnel-server` 使用**独立的 host key 与 authorized_keys**，与系统 sshd 隔离
- 若需进一步收敛暴露面，可用防火墙限制 2222 的来源 IP

#### 隧道使用专用密钥，不复用服务器登录密钥

**决策：新生成一把 `tunnel_key` 分发给各客户端，不使用云厂商签发的登录密钥（如 `key1.pem`）。**

一把私钥能做什么，取决于其公钥登记在**哪个文件**里。两份 `authorized_keys` 互不相干：

```
├── 系统 sshd（22）  → /root/.ssh/authorized_keys         ← key1.pem，可开 root shell
└── tunnel-server（2222）→ /etc/tunnel-server/authorized_keys ← tunnel_key，仅可开隧道
```

若复用 `key1.pem`，则**每台装了 devhelper 的机器都自动获得服务器 root 权限**，
且某台泄露时无法单独撤销。专用密钥的工作量与之相同（同样是生成一把、拷给各机器，
客户端无需额外操作），但权限小得多，撤销时只需删一行。

**权限隔离由代码保证，非配置约束**——`server.go` 的 channel 分发只认
`tunnel-ctrl@devhelper` 与 `direct-tcpip`，`session`（shell/exec/sftp 的入口）
落进 `default` 被拒绝。比 `ForceCommand` 之类"允许但约束"更彻底。

已由 `internal/server/security_test.go` 实测验证：

| 验证项 | 实测结果 |
|---|---|
| 隧道密钥开 shell | `rejected: unknown channel type (session)` |
| 借服务端连 `192.168.1.1`、`10.0.0.1` | `administratively prohibited` |
| 借服务端连 `169.254.169.254`（云元数据服务） | 同上被拒 |
| 未登记公钥认证 | `公钥未被授权` |

这些测试同时充当护栏：将来若有人为调试给服务端加上 session 支持，测试会失败并
提示该改动破坏了权限隔离。

**服务端运行于 Linux**。因不使用 Fyne，服务端**无需 CGO**，可从 Windows 开发机
直接交叉编译为纯静态 ELF（~4.5MB，不依赖 glibc）。部署细节见 `devhelper/DEPLOY.md`。

#### 补充：多路复用的原理

需区分两种"复用"，它们不是一回事：

```
第一种：多个 TCP 连接打到同一端口（任何 TCP 服务器都能做，与 SSH 无关）
  客户端A ──TCP连接1──┐
  客户端B ──TCP连接2──┼──► 服务器 :22（sshd 为每个连接 fork 一个进程）
  客户端C ──TCP连接3──┘

第二种：一条 TCP 连接内部再分出多条逻辑流  ← 本设计依赖的是这个
  客户端A ──TCP连接1──► 服务器 :22
                │
                ├─ channel #1: 端口转发（-R）
                ├─ channel #2: 端口转发（-L）
                ├─ channel #3: shell 会话
                └─ channel #4: 控制通道
                四条同时活着，互不干扰，共用这一条 TCP 连接
```

第二种是 SSH 协议规范（RFC 4254）明确定义的能力，不是技巧或 hack。

**SSH 协议分层**：

```
┌──────────────────────────────────────────┐
│ 连接层 (Connection)  ← 多路复用在这一层   │
│   channel #1, #2, #3, #4 ...             │
├──────────────────────────────────────────┤
│ 认证层 (Authentication)  ← 公钥认证，一次 │
├──────────────────────────────────────────┤
│ 传输层 (Transport)       ← 加密、完整性   │
├──────────────────────────────────────────┤
│ TCP                      ← 一条连接，:22  │
└──────────────────────────────────────────┘
```

连接层做的事与 TCP 在 IP 之上做的几乎一致：

| TCP 在 IP 之上 | SSH 连接层在加密流之上 |
|---|---|
| 端口号区分流 | **channel ID** 区分流 |
| 三次握手 | `CHANNEL_OPEN` / `CHANNEL_OPEN_CONFIRMATION` |
| 滑动窗口流控 | `CHANNEL_WINDOW_ADJUST` |
| `FIN` 关闭 | `CHANNEL_CLOSE` |
| 数据分段 | `CHANNEL_DATA` |

线路上（解密后）实际流过的是带标签的消息包：

```
[CHANNEL_DATA, ch=1, "GET /index.html..."]   ← 端口转发数据
[CHANNEL_DATA, ch=4, "{\"type\":\"hello\"}"]  ← 控制消息
[CHANNEL_DATA, ch=1, "HTTP/1.1 200 OK..."]   ← 转发响应
[CHANNEL_WINDOW_ADJUST, ch=1, 65536]         ← 流控
```

接收方按 ID 分拣入对应队列。原理与交换机看 MAC 转发、TCP 看端口分流相同。

#### 补充：为何控制通道不套用 WebSocket

需区分两个问题：

**Q: WebSocket 能跑在 SSH 隧道里吗？** 能，且毫无特殊之处。WS 就是 TCP 之上的协议，SSH 端口转发转发的就是 TCP 流。SSH 在此是**透明水管**，不关心内容是 HTTP、WS 还是别的。

**Q: 控制通道该用 WebSocket 吗？** **不该。** 想要 WS 的那些特性，SSH channel 原生已有：

| 想要 WS 的原因 | SSH channel |
|---|---|
| 服务端主动推送 | ✅ 双向全双工，服务端随时可写 |
| 长连接免重复握手 | ✅ channel 开着就一直在 |
| 加密 | ✅ SSH 传输层已加密 |
| 认证 | ✅ SSH 公钥认证已完成 |
| 消息边界 | ⚠️ 需自己分帧（见 §5） |

套娃的代价：

```
❌ 你的消息 → WS帧 → HTTP Upgrade握手 → TCP → SSH channel → SSH加密 → TCP
                          ↑ 这层的加密/认证/连接管理全是重复的
                          ↑ 且服务端要跑 HTTP 服务器处理 Upgrade——正是"怕自写代码有洞"想避免的东西

✅ 你的消息 → JSON + \n → SSH channel → SSH加密 → TCP
```

WebSocket 存在的历史原因是浏览器只会说 HTTP，需要在 HTTP 基础设施上打洞。**本场景无浏览器**——是 Go 客户端与 Go 服务端对话，该约束不存在。

### 4.2 单连接多 channel，不建立两次 SSH 连接

**问题**：Importer 需要先知道有哪些 Exporter 在线，才知道要监听哪些本地端口。这个先后顺序是否意味着要建两次连接（一次查信息、一次做转发）？

**结论：不需要，且不应该。一次连接即可。**

**原因**：本地转发的监听端口是**连接建立后动态决定的**，不是连接参数。`x/crypto/ssh` 中本地转发的实现是：

```
1. ssh.Dial()                          → 建立连接（一次，仅此一次）
2. net.Listen("tcp", "127.0.0.1:80")   → 本地开监听（纯本地操作，与 SSH 无关）
3. 每来一个连接 → client.Dial(...)      → 在已有 SSH 连接里开 channel
```

第 2 步是本进程在本地 listen，与 SSH 连接无关，想什么时候做都行。

**时序**：

```
T0  软件启动    ssh.Dial() ──► 建立唯一 SSH 连接（认证一次），此后一直活着
T1  查询在线    OpenChannel("ctrl") ──► 返回注册表 JSON ──► UI 展示可选列表
T2  用户勾选    net.Listen("127.0.0.1:80")   ← 此时才决定端口，完全来得及
T3  开始访问    client.Dial(...)             ← 复用 T0 的连接，开新 channel
T4  再选一个    重复 T2/T3，仍复用 T0 的连接
```

**两次连接的实际坏处**：
- 密钥有 passphrase 时用户被问两次
- 连接1 查到的注册表到连接2 建立时可能已过期；单连接则可由服务端在注册表变化时**主动推送**，UI 实时更新
- 服务端无法关联两个连接是同一个 Importer，权限/审计/"谁在访问谁"都变复杂
- 断线重连时两条连接的状态机要同步，易出 bug

### 4.3 服务端端口由服务端动态分配

**决策**：Exporter 请求 `-R 0:localhost:80`（远端端口写 `0`），**服务端自动分配空闲端口并返回实际端口号**。客户端拿到后通过控制 channel 上报注册表。

**理由**：这是 SSH 原生支持的能力。不用自己写"查询占用→挑一个→绑定"的逻辑，因而不存在检查与绑定之间的竞态。

### 4.4 Importer 本地端口默认与源端口一致，但可编辑

注册表里的"源端口"字段用于给 Importer 提供默认值（Exporter 那边是 80，这边也监听 80，心智负担最小）。

**但不能硬性一致**，两个现实问题：
- 本地端口可能已被占用
- **Windows 上绑定 1024 以下端口需要管理员权限**

UI 行为：默认填源端口号，可编辑，绑定失败时提示换一个。

### 4.5 注册表：自报 + 自查结合

| 方式 | 优点 | 缺点 |
|---|---|---|
| 客户端自报 | 能带友好名称（"办公室PC / Web服务"） | 服务端要信任客户端 |
| 服务端自查 | 真实可靠 | 只有端口号，无语义 |

**采用两者结合**：自报提供语义（名称、描述），自查做校验（确认端口确实在转发、未被冒名）。

注册表数据模型：

```
┌─────────┬──────────┬────────────┬────────┬──────┐
│ 客户端  │ 服务名   │ 服务端端口 │ 源端口 │ 状态 │
├─────────┼──────────┼────────────┼────────┼──────┤
│ 办公室PC│ Web      │ 8080       │ 80     │ 在线 │
│ 办公室PC│ SSH      │ 8022       │ 22     │ 在线 │
│ 家里NAS │ 管理页   │ 9443       │ 443    │ 在线 │
└─────────┴──────────┴────────────┴────────┴──────┘
```

客户端断开即摘除。Importer 拉到此表后 UI 展示为可选列表，用户点选后自动建立对应的 `-L`，无需人工记忆 `80→8080` 这类映射关系。

### 4.6 每条隧道独立 goroutine

**决策**：每条隧道一个独立 goroutine，独立启停、独立重连、独立退避。

**对比现状**：现有脚本把三条 `-R` 塞进同一个 ssh 进程，配合 `ExitOnForwardFailure=yes`，只要一条转发的远端端口被占用，**整个进程退出、三条一起断**。

### 4.7 保持 `GatewayPorts no`（默认值）

服务器上 `-R` 绑定的端口默认只绑 `127.0.0.1`。**必须保持这个默认值。**

这意味着 Exporter 推上来的服务不会直接暴露在公网，只有能 SSH 进服务器的人（即通过 `-L` 的 Importer）才能访问。

**若开启 `GatewayPorts yes`，内网服务将直接裸奔在公网上。**

整个架构的安全性建立在一条原则上：**所有访问都必须先通过 SSH 认证**。这与 4.1"不额外开端口"是同一个原则的两面。

---

## 5. 控制协议

承载于 §4.1 的 SSH channel 之上。服务端程序的开工前置。

### 5.1 编码：JSON Lines

**每条消息一行 JSON，`\n` 分隔。**

```go
// 发送：encoding/json 自动加 \n，且会转义消息体内的换行
json.NewEncoder(ch).Encode(msg)

// 接收
scanner := bufio.NewScanner(ch)
for scanner.Scan() { json.Unmarshal(scanner.Bytes(), &msg) }
```

理由：

- **可调试**——出问题时可把原始消息直接打进 UI 日志面板肉眼查看，这在本项目里价值很高
- **可演进**——加字段不破坏旧版本（旧版忽略未知字段）
- 十行代码，无额外依赖

未采用：长度前缀（`encoding/json` 已转义换行，裸 `\n` 风险不存在）；protobuf/msgpack（控制消息量极小，省下的字节无意义，却引入代码生成或依赖）。

> **注意**：SSH channel 提供的是**字节流**（如 TCP），不是**消息流**（如 WebSocket）。必须自行分帧，否则会读到粘包/半包。JSON Lines 即本项目的分帧方案。

### 5.2 同步语义：全量快照

**本协议只有一种同步语义——所有状态传递都是全量快照，不用增量。**

适用于 Exporter 上报隧道列表、服务端下发注册表两处。

理由：增量同步一旦丢失一条消息，两端将**永久不一致且难以察觉**；全量快照天然自愈。本项目隧道数与在线机器数均为个位数，全量开销可忽略（注册表即使 50 条也仅几 KB）。

**这条决策显著简化服务端**：服务端无需维护"我上次告诉过谁什么"。

### 5.3 消息格式

所有消息含 `v`（协议版本）和 `type`（消息类型）两个公共字段。

#### 握手（连接后第一条，双向）

```jsonc
// C→S
{"v":1,"type":"hello","role":"exporter","id":"a3f8...","name":"办公室PC",
 "client_version":"0.1.0"}
// S→C
{"v":1,"type":"hello_ok","server_version":"0.1.0"}
```

- `role`：`"exporter"` | `"importer"`
- `id`：UUID，见 §7.8
- **Importer 同样要发 `hello`**（见 §5.6）

#### Exporter 上报隧道（C→S，全量）

必须在 `-R` 建立、拿到服务端分配的实际端口之后发送：

```
1. Exporter 请求 -R 0:localhost:80  →  服务端返回实际端口 8080
2. Exporter 上报："我有这些隧道：…"
3. 服务端写入注册表
```

```jsonc
// C→S —— 每次变化都发完整列表，不是增量
{"v":1,"type":"publish","tunnels":[
  {"src_port":80,"remote_port":8080,"name":"Web服务"},
  {"src_port":22,"remote_port":8022,"name":""}
]}
// S→C
{"v":1,"type":"publish_ok"}
```

- `name`：**隧道名，可选**。空则 UI 显示端口号。
  常见端口（80/22）尚可自解释，但 `8020`、`5007` 这类过几天自己也认不出，故保留此字段。
  注意与机器名（`hello.name`）区分：Importer 侧显示为「办公室PC / Web服务」。

#### 注册表下发（S→C，全量）

Importer 首次 `hello_ok` 后收到一次，此后**每次注册表变化服务端主动推送完整快照**：

```jsonc
{"v":1,"type":"registry","entries":[
  {"id":"a3f8...","name":"办公室PC","src_port":80,"remote_port":8080,
   "tunnel_name":"Web服务","since":"2026-08-05T13:38:02Z",
   "client_version":"0.1.0"}
]}
```

| 字段 | 用途 |
|---|---|
| `id` | §7.7 重连匹配依据 |
| `name` | UI 显示机器名 |
| `src_port` | §7.7 匹配依据 + Importer 本地端口默认值（§4.4） |
| `remote_port` | Importer 实际要连的服务端端口 |
| `tunnel_name` | 显示用，可为空 |
| `since` | 上线时间，可看出"刚上线"还是"挂了很久" |
| `client_version` | 排查版本不一致问题 |

> 未采用 `last_seen`：注册表是实时的，在线即在线，掉线即摘除。

#### 错误（S→C）

```jsonc
{"v":1,"type":"error","code":"duplicate_id","msg":"该 UUID 已被另一连接注册"}
```

### 5.4 应答机制

**每条 C→S 请求都有对应响应**（`*_ok` 或 `error`）。

理由：SSH 保证字节送达，但**送达 ≠ 服务端接受**。例如服务端可能因 UUID 冲突而拒绝注册——无回执时 Exporter 会误以为注册成功，而 Importer 侧根本看不到它，排查极其困难。

### 5.5 错误码

**错误必须带机读的 `code`，不能只有人读的 `msg`。**

因为**服务端错误同样要参与 §7.2 的失败分类**——客户端需据此判断"重试还是停下报错"。若靠匹配 `msg` 文本判断，服务端改个措辞就会让客户端分类逻辑静默失效（不报错，只是行为变得不对）。

| `code` | 分类（§7.2） | 含义与处理 |
|---|---|---|
| `version_mismatch` | 不可自愈 | 协议版本不兼容，提示用户升级 |
| `duplicate_id` | 不可自愈 | UUID 已被占用，提示"是否复制了 config.json？" |
| `port_alloc_failed` | **可自愈** | 服务端分配端口失败，退避重试 |
| `server_busy` | **可自愈** | 服务端繁忙，退避重试 |
| `bad_request` | 不可自愈 | 消息格式错误，属客户端 bug |
| `internal_error` | **可自愈** | 服务端内部错误，退避重试 |

### 5.6 版本协商

**连接后第一条消息即交换版本，不匹配立即报错并终止。**

成本极低（一条消息 + 一次整数比较），但可消除一整类难查问题：本工具会被拷到多台机器，"A 机器是上周的版本、B 机器是今天编译的"极易发生。无协商时症状是"莫名其妙不工作"，有协商时是明确的"客户端版本过旧，请升级"。

### 5.7 Importer 也发送 hello

**Importer 需向服务端表明身份**（`role` + `id` + `name`），尽管它只是注册表的消费者。

- 成本为零——`hello` 消息本来就要发
- 服务端得以知道"当前有哪些 Importer 在线"，为将来的审计留下基础
- 否则服务端日志里只能看到"有个连接"，无法回答"上周三谁访问了办公室PC"

**但 Importer 不上报"正在连谁"**。因此 §7.6 的对端离线通知采用**广播**而非精准推送：注册表一有变化就把新快照发给所有在线 Importer，各 Importer 自行比对"我连的那个 UUID 还在吗"——不在则标黄"对端离线"。

这一简化由 §5.2 的全量快照语义直接支撑：服务端不需要知道谁关心谁。

---

## 6. UI 设计

**两种隧道分置于两个页签**，而非单列表加"类型"列。

```
┌─ DevHelper 隧道管理器 ───────────────[_][□][×]┐
│  ⚠ 连接已断开：网络不可达 —— 12s 后重试        │ ← 连接横幅(§8.9)
│                                                │
│  ┌ 连接配置 ────────────────────────────────┐  │
│  │ 主机  [root@tunnel.example.com          ]     │  │  widget.Entry
│  │ 密钥  [C:\...\key1.pem      ] [浏览...]  │  │  Entry + Button(FileDialog)
│  └──────────────────────────────────────────┘  │
│                                                │
│  ┌──────────┬──────────┐                       │  container.AppTabs
│  │ 【导出】 │  导入    │                       │
│  ├──────────┴──────────────────────────────┐  │
│  │  [+ 添加] [编辑] [删除]                  │  │
│  │                                          │  │  widget.List
│  │  ● NAS      服务端 15007 ← 127.0.0.1:5007│  │  (每行自定义布局)
│  │  ● 设备-29  服务端 18080 ← 192.168.1.29:8020│ │
│  │  ○ 设备-27  服务端 待分配 ← 192.168.1.27:8021│ │
│  └──────────────────────────────────────────┘  │
│                                                │
│  ┌ 日志 ──────[等级▾][☐自动滚动]──[清空]──┐  │
│  │ 13:38:02 [NAS] 已连接                    │  │  widget.List（可着色）
│  │ 13:38:05 [设备-29] 认证失败: 密钥被拒绝  │  │
│  └──────────────────────────────────────────┘  │
└────────────────────────────────────────────────┘

「导入」页签（内容不同，操作也不同）：
  ┌──────────┬──────────┐
  │  导出    │ 【导入】 │
  ├──────────┴──────────────────────────────┐
  │  [+ 从在线列表添加] [编辑] [删除]        │ ← 不是手填，是从注册表选
  │                                          │
  │  ● 办公室PC/Web  本地 80  → 办公室PC:80  │
  │  ◌ 家里NAS/管理  本地 8443 → 家NAS:443   │ ← ◌ 对端离线(§8.6)
  └──────────────────────────────────────────┘
```

### 为何用页签而非单列表

两侧的**操作方式本质不同**，混在一个列表里会让工具栏语义含糊：

| | 导出（Export） | 导入（Import） |
|---|---|---|
| 添加方式 | 手工填写本地端口 | **从服务端在线列表中选择**（§4.2 T1-T2） |
| 远端端口 | 服务端自动分配（§4.3） | 由注册表给出，不可填 |
| 依赖 | 无，可独立工作 | 控制通道 + 服务端注册表 |
| 独有状态 | — | 对端离线（§8.6） |

「添加」按钮在两侧的行为完全不同——导出是弹表单，导入是弹在线列表选择器。
单列表下这个按钮无法自解释。

### 两个 Fyne 特有的实现要点

**1. 日志面板用 `widget.List` 而非 `widget.Entry(MultiLine)`**

`Entry` 在日志量大时明显卡顿，且无法给错误行着色。用 `List` + 自定义行渲染可实现按等级着色（ERROR 红、WARN 黄），直接服务"可辨别的故障日志"这一诉求。

代价是要自己管环形缓冲（保留最近 ~2000 行），这顺带解决了内存无限增长问题。

**2. UI 更新必须回到主线程**

与 WinForms 的 `Invoke` 同理：隧道 goroutine **不能**直接修改控件。

标准结构：隧道 goroutine 把日志/状态事件发进 channel → UI 侧统一消费者读 channel → 调用 `Refresh()`。

**这个结构必须一开始就定好，后期改动代价很高。**

---

## 7. 相比现有脚本要修正的问题

以下问题在 Go 方案中**大多是结构上就不存在**，而非需要额外写代码修补。

| # | 现状问题 | 位置 | Go 方案 |
|---|---|---|---|
| 1 | 依赖 `ssh.exe` 在 PATH | `start_forwarding_dbg.ps1:73` | 不需要，SSH 在库里 |
| 2 | stderr 完全丢失，排障只能靠 ping 猜 | `:132` `Start-Process -WindowStyle Hidden` | SSH 库直接返回结构化 error |
| 3 | `Get-Process ssh \| Stop-Process` 杀掉机器上**所有** SSH 进程 | `stop_forwarding.ps1:32` | 隧道是进程内 goroutine，只关自己的 |
| 4 | 三条转发挤一个进程，一损俱损 | `:41-45` + `ExitOnForwardFailure=yes` | 每条隧道独立 goroutine |
| 5 | 固定 5 秒重连、无退避、无限刷日志 | `:149` | 每条隧道独立指数退避 |
| 6 | 日志"轮转"实为 `Clear-Content` 清空，丢弃历史 | `:60` | 滚动保留数份 |
| 7 | 配置硬编码 | `:32-45` | `config.json` + UI 读写 |
| 8 | `StrictHostKeyChecking=no`，放弃中间人防护 | `:116` | 显式 `HostKeyCallback`：首次连接确认指纹并存储，之后校验 |
| 9 | 私钥文件权限过宽被 OpenSSH 拒绝加载 | `:121` `-i key1.pem` | 自行检查 ACL + UI 一键修复，见 §7.1 |

### 关于 #8

关闭主机密钥校验等于放弃对中间人攻击的防护。Go 方案中应实现：首次连接时 UI 弹窗展示指纹让用户确认 → 写入 known_hosts → 后续连接严格校验，不匹配则拒绝并告警。

### 关于 #5 的退避策略

需区分两类失败，详见 §8 重连状态机。现有脚本对两者一视同仁，认证失败会每 5 秒重试一次直到永远。

### 7.1 私钥文件权限（Windows）

**现象**（使用 `ssh.exe` 时）：

```
Bad permissions. Try removing permissions for user: NT AUTHORITY\Authenticated Users (S-1-5-11)
on file D:/EntTools/SSHtunnel/key1.pem.
@@@ WARNING: UNPROTECTED PRIVATE KEY FILE! @@@
Permissions for 'key1.pem' are too open. This private key will be ignored.
```

**这个报错在新方案中不会出现。** 该检查是 **OpenSSH 客户端自身的策略**——`ssh.exe` 读取私钥前先检查文件 ACL。`golang.org/x/crypto/ssh` **不做此检查**：`os.ReadFile` + `ssh.ParsePrivateKey` 全程不关心文件权限。

**但"报错消失"不等于"问题消失"。** OpenSSH 警告的是一件真事：`Authenticated Users` 可读意味着**本机任何登录用户都能拷走私钥**，进而以你的身份连上服务器。本项目中该风险被放大：

- exe 设计为"拷过去双击就用"，很可能落在 `D:\` 这类默认 ACL 宽松的目录
- 目标机是临时的/别人的开发机（§1），可能确有其他用户
- 这把私钥通向公网服务器，而服务器背后是整个内网

**决策：自行检查 ACL，但做得比 OpenSSH 好用。**

OpenSSH 的缺陷在于只会拒绝、然后甩一句 `Try removing permissions for...`，剩下的要用户自己去 `icacls` 折腾。本项目有 GUI，应当直接把事办了：

```
选择密钥 / 启动时 → 检查 pem 的 ACL
   ├─ 仅当前用户 + SYSTEM 可读 → 放行
   └─ 存在其他可读者 → 弹窗（提示，非拒绝）

        ┌────────────────────────────────────────────┐
        │ ⚠ 私钥 key1.pem 可被本机其他用户读取       │
        │                                            │
        │ 当前可读者：                               │
        │   NT AUTHORITY\Authenticated Users         │
        │                                            │
        │      [ 自动修复权限 ]   [ 忽略并继续 ]      │
        └────────────────────────────────────────────┘
```

- **自动修复**：调 `icacls` 断开继承，仅保留当前用户与 SYSTEM。一次点击解决，无需用户查命令
- **忽略并继续**：保留退路。临时机器、用完即删等场景下用户可能确实不在乎

**采用「提示 + 可忽略」而非「直接拒绝」**：有 GUI 可提供一键修复，体验优于 OpenSSH 的硬拒绝；同时保留"我知道我在干什么"的出口，避免在临时机器上被卡死。

> **暂不实现**「私钥不落盘」方案（从 Windows 凭据管理器读取，或首次粘贴后加密存入 config）。它能从根上消除此问题，但与"拷过去双击就用"的诉求冲突（每台机器需重新录入一次）。列为将来可选项。

---

## 8. 重连状态机

### 8.0 两个层次

有两层东西会断，重连策略不同：

```
第一层：SSH 连接本身（全局，仅一条）
  断了 → 所有隧道的 channel 全部失效，必须先救回连接

第二层：单条隧道（多条，各自独立）
  连接正常，但这一条转发失败（对端离线、本地端口被占等）
  → 只影响它自己
```

### 8.1 断线检测：心跳

TCP 连接断开未必会立刻报错——对端断电、拔网线、NAT 悄悄丢弃映射时，本地 socket 可能长时间"看起来正常"。必须主动心跳。

**参数：间隔 15s，连续 3 次无响应判定断开（约 45 秒内发现）。**

- 现有脚本是 `ServerAliveInterval=30` + `ServerAliveCountMax=3`（约 90 秒），对远程开发调试场景太慢
- 更激进（如 10s/2 次）容易被网络抖动误判，引发不必要的重连

实现：`x/crypto/ssh` 的 `SendRequest` 发送 keepalive 请求。

### 8.2 失败分类

这是整个状态机的核心。**"能否靠等待自行恢复"是唯一判据。**

| 失败情形 | 处理 | 理由 |
|---|---|---|
| 网络不通 / 服务器无响应 | **重试** | 网络恢复即好 |
| 连接被拒绝（sshd 挂了） | **重试** | 服务器重启后恢复 |
| 心跳超时 | **重试** | 同上 |
| 对端 Exporter 离线 | **重试** | 对端开机即恢复，见 §7.6 |
| 远端端口分配失败 | **重试** | 重试可拿到新端口，见下 |
| 密钥文件不存在 / 格式错误 | **停止** | 不改配置永不会好 |
| 认证被拒绝（密钥未授权） | **停止** | 重试一万次也没用 |
| **主机密钥不匹配** | **停止 + 显著告警** | 可能是中间人攻击，**绝不能自动重试** |
| 本地端口被占用（Importer） | **停止** | 需用户处理，见 §7.5 |
| 本地端口需管理员权限（<1024） | **停止** | 需用户提权或换端口 |

> **远端端口不允许手动指定。** 全部服从服务端动态分配（§4.3）。这消除了"远端端口冲突"这一整类问题——分配失败只需重试一次即可拿到新端口，无需按"手动指定 vs 自动分配"区分两套逻辑。

### 8.3 退避曲线

**可自愈失败：5s → 10s → 20s → 40s → 80s → … → 上限 5min，永不放弃。**

不设次数上限。使用场景（笔记本合盖外出、服务器维护重启）里"最终能自动恢复"远比"及时放弃"重要——最坏情况等待 5 分钟，无需任何手动干预。

**不可自愈失败：立即停止，不重试**，UI 标红并显示具体原因。

### 8.4 重连成功后的隧道恢复

**规则：重建所有"用户意图是运行"的隧道，但跳过因不可自愈原因停止的。**

此规则直接由 §7.2 的分类推导，不是独立决策：

- 因**可自愈**原因失败的（对端关机、网络不通）→ 照常重建。这类隧道本来就在退避重试，且外出一趟回来后对端很可能已恢复
- 因**不可自愈**原因停止的（认证失败、端口需提权）→ 保持停止。重建只会再次失败并刷日志
- 用户**主动停止**的 → 保持停止，尊重用户意图

### 8.5 本地监听端口：断线时关闭

**决策：SSH 连接断开时，Importer 的本地 listener 一并关闭。**

| | 保留监听 | **关闭监听（采用）** |
|---|---|---|
| 用户看到 | 网页转圈 / 502，不易判断是隧道问题 | "无法连接"，故障现象明确 |
| 恢复时 | 立即可用 | 需重新绑定，**可能已被其他程序抢占** |

选择关闭，是为了让故障现象一眼可辨。

**代价及其处理**：端口被抢占从"风险"变成必须处理的常规情况。因此——

> **重连后绑定失败时，错误信息必须精确到占用者。**
> 不能只报"端口 80 被占用"，必须显示"端口 80 已被 nginx.exe (PID 1234) 占用"。
> Windows 上可通过 `GetExtendedTcpTable` 拿到占用端口的 PID 与进程名。
> 否则用户只知道绑不上、不知道该去处理谁，"用户自行处理"就无从谈起。

### 8.6 对端 Exporter 离线

区别于"自己断线"：本地连接和服务器都正常，但对面那台 Exporter 掉线了。

```
Importer ══SSH══► 服务器 S ◄══SSH══ Exporter
  连接正常          正常            ✕ 断电

本地监听还在，但转到服务端端口后那边已无人
```

**处理**：服务端在注册表变化时**主动推送**通知给相关 Importer → UI 标黄"对端离线" → 按 §7.5 关闭本地监听 → 对端恢复后自动重建。

这正是坚持"单连接 + 服务端主动推送"（§4.2）的价值：服务端知道谁掉线，能第一时间告知，用户无需靠猜。

### 8.7 重连后的对端匹配

服务端端口是动态分配的（§4.3），**重连后 Exporter 拿到的服务端端口号大概率会变**：

```
断线前：Importer 本地 :80 ──► 服务端 :8080 ──► 办公室PC :80
断线后：Importer 本地 :80 ──► 服务端 :8091 ──► 办公室PC :80
                                    ↑ 变了
```

**匹配规则：按「Exporter 身份 + 源端口」匹配，而非按服务端端口号。**

Importer 记住的是"我连的是 办公室PC 的 80 端口"，重连后查新注册表找到对应条目，取其新的服务端端口。用户完全无感。

按服务端端口号记忆的方案在动态分配下必然失效。

### 8.8 Exporter 身份标识

§7.7 的匹配依赖一个稳定 ID。**采用「计算机名 + UUID」双字段**：

| 字段 | 来源 | 用途 |
|---|---|---|
| 显示名 | 自动取计算机名，用户可改 | UI 显示、日志可读性 |
| UUID | 首次启动生成，存 `config.json`，永不变 | **实际匹配依据**，UI 永不显示 |

**为何不能只用计算机名**：Windows 默认名（`DESKTOP-XXXXXXX`）重名概率低，但用户手动改名（`DEV-PC`、`WIN10`）或从同一镜像克隆的机器/虚拟机，重名很常见。重名会导致 §7.7 匹配到错误的机器。

**为何不能只用 UUID**：`a3f8-...` 对用户不可读，日志里无法辨认。

双字段兼得"零配置开箱即用"（自动取机器名，无需填写）与"匹配绝对可靠"（走 UUID）。用户随时可改显示名，不影响已建立的匹配关系。

### 8.9 UI 状态分档

隧道行状态（Exporter / Importer 通用）：

| 状态 | 颜色 | 含义 |
|---|---|---|
| ● 运行中 | 绿 | 一切正常 |
| ◐ 重连中 | 黄 | 可自愈失败，退避重试中（显示"12s 后重试"） |
| ○ 已停止 | 灰 | 用户主动停止 |
| ✕ 错误 | 红 | 不可自愈，已放弃，需用户处理（并列显示原因） |

**连接层故障用顶部横幅，不逐行标黄。**

SSH 连接断开是全局性问题，在每一行重复标黄是噪音，且会让用户误以为"三条隧道各自都坏了"。顶部横幅（"连接已断开，45s 后重试…"）能一眼区分"总连接的问题"与"单条隧道的问题"。

---

## 9. 组件结构

```
tunnel.exe   单文件 ~20MB，无任何运行时依赖
├── Fyne GUI（独立窗口，双击即用）
├── SSH 连接层 (x/crypto/ssh)  ← 目标机不需要装 OpenSSH
│     ├── 单连接，认证一次
│     ├── 控制 channel（查询注册表 / 上报映射）
│     ├── Exporter 隧道 goroutine ×N（client.Listen）
│     └── Importer 隧道 goroutine ×N（net.Listen + client.Dial）
├── 事件 channel → UI 主线程消费 → 日志面板着色渲染
├── config.json（exe 同目录）
└── known_hosts（exe 同目录）

tunnel-server.exe   部署在服务器（暂未开工）
├── 维护在线 Exporter 注册表（纯内存，见 §9.1）
├── 处理控制 channel 请求
└── 注册表变化时向所有在线 Importer 广播全量快照（§5.7）
```

### 9.1 注册表纯内存，不持久化

**决策：注册表只存在于内存，服务端重启即清空。**

注册表描述的是"**当前谁在线**"，这个信息天然依附于活动的 SSH 连接。服务端重启后所有连接均已断开，注册表本就应当是空的。

落盘反而有害：恢复出来的条目全是"看起来在线、实际已断"的僵尸，Importer 照着连接必然失败，且需额外编写清理逻辑。

> **预留（暂不实现）**：可另做一份**历史审计日志**（"办公室PC 于 8月5日 13:38–18:20 在线"）。它与在线注册表完全分离，不参与在线判断，因此不影响本决策的简洁性。

### 9.2 多 Importer 并发访问同一 Exporter：不做限制

**决策：完全允许，不加任何限制或特殊逻辑。**

技术上无冲突——服务端的转发端口可接受多个并发连接，SSH 为每个连接开独立 channel，互不干扰（如同多人同时访问同一网站）。

且这是**正常且有用**的场景：用户可能同时用笔记本和台式机访问同一台内网设备。

**未采用「Exporter 侧显示谁在访问我」**：该功能需 Importer 上报"正在连谁"，与 §5.7 的决策冲突，需改协议并让 Importer 维护动态上报，而收益仅是信息展示。若将来确有需要，服务端可自行统计连到该端口的 channel 数，**无需改协议**。

### 9.3 系统托盘：关闭窗口即最小化

**决策：关闭窗口 = 最小化到托盘；退出需从托盘菜单执行。**

符合常驻工具惯例——本工具的使用模式是"开着就一直开着"，隧道需持续可用，不会频繁开关。

**托盘图标兼做全局状态指示**：绿=全部正常 / 黄=有隧道重连中 / 红=有隧道处于错误状态。扫一眼任务栏即知情况，无需切换窗口。

> **必须配套**：首次点击关闭时弹出一次性提示——"程序将继续在后台运行，可从托盘图标退出"，并提供"不再提示"勾选。
> 否则本方案最大的隐患就是用户以为已关闭、实际隧道仍在运行。

实现：Fyne 的 `desktop.App` → `SetSystemTrayMenu`。

### 9.4 强制单实例

**决策：第二个实例启动时，将已有实例窗口唤至前台，然后自身退出。**

在本项目中这不仅是体验问题，**会真实出错**：

```
实例1 运行中，Importer 监听 127.0.0.1:80
实例2 启动，读同一份 config.json，也尝试监听 127.0.0.1:80
     → 绑定失败，UI 报"端口被占用"
     → 用户排查后发现占用者是自己的另一个实例
```

且两个实例会以同一 UUID 连接服务端，触发 §5.5 的 `duplicate_id`。

**与 §9.3 的托盘是配套的**：窗口藏在托盘中时，用户很自然会去双击 exe——此时"唤起已有窗口"正是其真实意图。

实现：创建具名 Mutex；第二个实例发现 Mutex 已存在，即通过窗口消息唤起对方并退出。

> **已确认**：**一个实例只连接一台服务器**，无同时连接多台服务器的需求。

---

## 10. 待定事项

设计阶段的所有既定议题均已完成：

1. ~~重连状态机细节~~ —— **见 §8**
2. ~~控制协议消息格式~~ —— **见 §5**（多路复用原理见 §4.1）
3. ~~服务端注册表的持久化与否~~ —— **见 §9.1**
4. ~~多 Importer 并发访问同一 Exporter 的行为~~ —— **见 §9.2**
5. ~~是否需要系统托盘~~ —— **见 §9.3**
6. ~~单实例约束~~ —— **见 §9.4**

### 已确认事项

| 项 | 位置 | 结论 |
|---|---|---|
| 一个实例只连一台服务器 | §9.4 | **确认** |
| 私钥存放方式 | §7.1 | **落盘，与 exe 同目录**。不做凭据管理器/加密存储 |
| Exporter 侧显示"谁在访问我" | §9.2 | **不做**。将来若需要，服务端可自行统计 channel 数，无需改协议 |
| 日志环形缓冲 ~2000 行 | §6 | **确认** |
| 退避上限 5min | §8.3 | **确认** |
| 应用名 | — | **devhelper** |
| 控制 channel 名称 | §4.1 | `tunnel-ctrl@devhelper` |
| 协议版本 | §5.3 | 起始 `v:1`，靠 JSON 加字段向后兼容 |
| 开发机 CGO 工具链 | §2 | **确认就绪** |

> **私钥落盘的直接后果**：`key.pem` 与 exe 同目录，一并被拷贝到各台目标机。
> 这使 **§7.1 的 ACL 检查与一键修复成为必需项而非可选项**——它是本方案下私钥泄露的唯一防线。

### 预留项（暂不实现）

| 项 | 位置 | 说明 |
|---|---|---|
| 服务端历史审计日志 | §9.1 | 与在线注册表分离，不参与在线判断 |
